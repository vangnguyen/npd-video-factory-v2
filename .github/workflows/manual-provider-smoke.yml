name: Retired paid-provider smoke (blocked)

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Fail closed - paid execution belongs to the dedicated plane
        run: |
          echo 'BLOCKED_PRE_CALL: legacy hosted provider smoke retired; provider calls=0'
          exit 1
