# NPD Video Factory V2 API

V2-01 exposes strict create-job, job-status, health, capability and recorded-artifact
endpoints. See `docs/API.md` at the repository root. It owns no AgentHub integration and
contains no publishing endpoint.
